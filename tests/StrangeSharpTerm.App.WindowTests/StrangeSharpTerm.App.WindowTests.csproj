<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
    <RootNamespace>StrangeSharpTerm.App.WindowTests</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="xunit.v3" />
    <PackageReference Include="Shouldly" />
    <PackageReference Include="Avalonia.Headless" />
  </ItemGroup>

  <ItemGroup>
    <!-- The same double the other test project uses. One file rather than two
         copies: what a host can be asked for is one interface. -->
    <Compile Include="../StrangeSharpTerm.App.Tests/FakeSessions.cs" Link="FakeSessions.cs" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../../src/StrangeSharpTerm.App/StrangeSharpTerm.App.csproj" />
  </ItemGroup>
</Project>
