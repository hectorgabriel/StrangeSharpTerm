namespace StrangeSharpTerm.Assist.Tests;

// Replaced as the ported suites land. Present so the project compiles and CI is
// green from the first commit rather than after the first port.
public class PlaceholderTests
{
    [Fact]
    public void ProjectBuilds() => true.ShouldBeTrue();
}
